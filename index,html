<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Qudus A. | Growth Architect</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-bg: #0a0a0a;
            --secondary-bg: #1a1a1a;
            --accent-color: #3b82f6;
            --text-primary: #ffffff;
            --text-secondary: #a0a0a0;
            --border-color: #2a2a2a;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: var(--primary-bg);
            color: var(--text-primary);
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Navigation */
        nav {
            background-color: rgba(10, 10, 10, 0.95);
            backdrop-filter: blur(10px);
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            border-bottom: 1px solid var(--border-color);
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 1rem 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--text-primary);
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
            align-items: center;
        }

        .nav-links a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: color 0.3s;
            font-size: 0.95rem;
        }

        .nav-links a:hover {
            color: var(--text-primary);
        }

        /* Hero Section */
        .hero {
            padding: 120px 20px 80px;
            text-align: center;
            background: linear-gradient(180deg, var(--primary-bg) 0%, var(--secondary-bg) 100%);
        }

        .hero-subtitle {
            color: var(--accent-color);
            font-size: 0.9rem;
            font-weight: 500;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 1rem;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #ffffff 0%, #a0a0a0 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-description {
            font-size: 1.1rem;
            color: var(--text-secondary);
            max-width: 700px;
            margin: 0 auto 2.5rem;
            line-height: 1.8;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 0.85rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            display: inline-block;
        }

        .btn-primary {
            background-color: var(--accent-color);
            color: white;
        }

        .btn-primary:hover {
            background-color: #2563eb;
            transform: translateY(-2px);
        }

        .btn-secondary {
            border: 1px solid var(--border-color);
            color: var(--text-primary);
        }

        .btn-secondary:hover {
            border-color: var(--accent-color);
            color: var(--accent-color);
        }

        /* Stats Section */
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            margin: 4rem 0;
        }

        .stat-card {
            background-color: var(--secondary-bg);
            padding: 2rem;
            border-radius: 12px;
            text-align: center;
            border: 1px solid var(--border-color);
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--accent-color);
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        /* Hero Image */
        .hero-image {
            margin: 4rem 0;
            text-align: center;
        }

        .hero-image img {
            max-width: 400px;
            width: 100%;
            border-radius: 16px;
            border: 2px solid var(--border-color);
        }

        /* Section Headers */
        .section {
            padding: 5rem 0;
        }

        .section-header {
            text-align: center;
            margin-bottom: 1rem;
        }

        .section-subtitle {
            color: var(--accent-color);
            font-size: 0.9rem;
            font-weight: 500;
            letter-spacing: 1px;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
        }

        .section h2 {
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        /* Expertise Grid */
        .expertise-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }

        .expertise-card {
            background-color: var(--secondary-bg);
            padding: 1.5rem;
            border-radius: 12px;
            border: 1px solid var(--border-color);
            text-align: center;
            transition: all 0.3s;
        }

        .expertise-card:hover {
            border-color: var(--accent-color);
            transform: translateY(-5px);
        }

        /* Featured Posts */
        .posts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2rem;
            margin-bottom: 2rem;
        }

        .post-card {
            background-color: var(--secondary-bg);
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid var(--border-color);
            transition: all 0.3s;
        }

        .post-card:hover {
            border-color: var(--accent-color);
            transform: translateY(-5px);
        }

        .post-category {
            background-color: rgba(59, 130, 246, 0.1);
            color: var(--accent-color);
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .post-content {
            padding: 2rem;
        }

        .post-card h3 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
            line-height: 1.4;
        }

        .post-card p {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            line-height: 1.7;
        }

        .post-tags {
            color: var(--accent-color);
            font-size: 0.85rem;
            margin-bottom: 1rem;
        }

        .post-link {
            color: var(--accent-color);
            text-decoration: none;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }

        .post-link:hover {
            text-decoration: underline;
        }

        .view-all {
            text-align: center;
            margin-top: 3rem;
        }

        /* Contact Section */
        .contact-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            max-width: 1000px;
            margin: 0 auto;
        }

        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }

        .contact-item h3 {
            font-size: 0.9rem;
            color: var(--text-secondary);
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .contact-item p, .contact-item a {
            color: var(--text-primary);
            font-size: 1.1rem;
            text-decoration: none;
        }

        .contact-item a:hover {
            color: var(--accent-color);
        }

        .contact-form {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .form-group label {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .form-group input,
        .form-group textarea {
            background-color: var(--secondary-bg);
            border: 1px solid var(--border-color);
            color: var(--text-primary);
            padding: 0.85rem;
            border-radius: 8px;
            font-family: inherit;
            font-size: 1rem;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--accent-color);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 150px;
        }

        /* Footer */
        footer {
            background-color: var(--secondary-bg);
            border-top: 1px solid var(--border-color);
            padding: 3rem 0;
            margin-top: 5rem;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .footer-info h3 {
            margin-bottom: 0.5rem;
        }

        .footer-info p {
            color: var(--text-secondary);
        }

        .footer-copyright {
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }

            .nav-links {
                gap: 1rem;
            }

            .contact-content {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            .posts-grid {
                grid-template-columns: 1fr;
            }

            .section h2 {
                font-size: 2rem;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2rem;
            }

            .hero-buttons {
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <div class="nav-container">
            <a href="/" class="logo">Qudus A.</a>
            <ul class="nav-links">
                <li><a href="#about">About</a></li>
                <li><a href="#featured">Featured</a></li>
                <li><a href="#contact">Contact</a></li>
                <li><a href="https://www.linkedin.com/in/qudus-damilare/" target="_blank">LinkedIn</a></li>
            </ul>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <p class="hero-subtitle">Growth Architect</p>
            <h1>Qudus Ahmod Damilare</h1>
            <p class="hero-description">
                Growth Architect with a unique blend of people-centric leadership, operational excellence, and data-driven decision-making. I bridge the gap between people, processes, and data to drive sustainable growth through innovation.
            </p>
            <div class="hero-buttons">
                <a href="#contact" class="btn btn-primary">Get in Touch</a>
                <a href="https://www.linkedin.com/in/qudus-damilare/" target="_blank" class="btn btn-secondary">LinkedIn</a>
            </div>

            <div class="stats">
                <div class="stat-card">
                    <div class="stat-number">500+</div>
                    <div class="stat-label">Connections</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">1,170+</div>
                    <div class="stat-label">Followers</div>
                </div>
            </div>

            <div class="hero-image">
                <img src="/images/qudus.png" alt="Qudus A. - Growth Architect">
            </div>
        </div>
    </section>

    <!-- Expertise Section -->
    <section id="about" class="section">
        <div class="container">
            <div class="section-header">
                <p class="section-subtitle">Expertise & Insights</p>
                <h2>Areas of Expertise</h2>
            </div>
            <div class="expertise-grid">
                <div class="expertise-card">Human Resources</div>
                <div class="expertise-card">Data Analytics</div>
                <div class="expertise-card">Administrative Operations</div>
                <div class="expertise-card">Operational Excellence</div>
                <div class="expertise-card">Talent Retention</div>
                <div class="expertise-card">Business Strategy</div>
            </div>
        </div>
    </section>

    <!-- Featured Posts Section -->
    <section id="featured" class="section">
        <div class="container">
            <div class="section-header">
                <p class="section-subtitle">Featured Content</p>
                <h2>Featured LinkedIn Posts</h2>
                <p style="color: var(--text-secondary); margin-bottom: 2rem;">Insights on leadership, business strategy, and professional growth in the Nigerian context.</p>
            </div>
            <div class="posts-grid">
                <article class="post-card">
                    <div class="post-category">Leadership</div>
                    <div class="post-content">
                        <h3>Why I Ask Janitors A C-Suite Question</h3>
                        <p>I've spent years as a Human resource professional, but my current focus is Administrative Operations. Recently, while recruiting for a janitor, I bypassed the standard interview questions and asked: "What would make you leave?" In Operations, consistency is the backbone of a healthy facility.</p>
                        <p class="post-tags">#AdminOps #HRBP #TalentRetention</p>
                        <a href="https://www.linkedin.com/posts/qudus-damilare_leadership-adminops-hrbp-activity-7415330132548132864-bB9_" target="_blank" class="post-link">Read on LinkedIn →</a>
                    </div>
                </article>

                <article class="post-card">
                    <div class="post-category">Business Strategy</div>
                    <div class="post-content">
                        <h3>Frameworks Don't Build Businesses. Principles Do</h3>
                        <p>Agile. Lean. Six Sigma. We love the vocabulary of success. But despite the sophisticated language, many businesses remain stagnant. The uncomfortable truth? We are often borrowing vocabulary without understanding substance.</p>
                        <p class="post-tags">#Leadership #NigeriaBusiness #GrowthMindset</p>
                        <a href="https://www.linkedin.com/feed/update/urn:li:activity:7414233116095205378/" target="_blank" class="post-link">Read on LinkedIn →</a>
                    </div>
                </article>

                <article class="post-card">
                    <div class="post-category">Entrepreneurship</div>
                    <div class="post-content">
                        <h3>Nigeria Businesses: Don't Copy Frameworks</h3>
                        <p>Systems are Contextual. Principles are Universal. In a business environment like Nigeria—where capital is tight and execution is everything—the cost of unnecessary complexity is too high. Stop playing "Business Framework Bingo."</p>
                        <p class="post-tags">#BusinessStrategy #Entrepreneurship</p>
                        <a href="https://www.linkedin.com/posts/qudus-damilare_businessstrategy-leadership-nigeriabusiness-activity-7413896822429065218-U3J8" target="_blank" class="post-link">Read on LinkedIn →</a>
                    </div>
                </article>
            </div>
            <div class="view-all">
                <a href="https://www.linkedin.com/in/qudus-damilare/" target="_blank" class="btn btn-secondary">View all posts on LinkedIn</a>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="section">
        <div class="container">
            <div class="section-header">
                <p class="section-subtitle">Get in Touch</p>
                <h2>Let's Build Something Great Together</h2>
                <p style="color: var(--text-secondary); margin-bottom: 2rem;">Whether you need help with HR strategy, data analytics, or operational excellence, I'm here to help you drive growth and create sustainable impact.</p>
            </div>
            <div class="contact-content">
                <div class="contact-info">
                    <div class="contact-item">
                        <h3>Location</h3>
                        <p>Lagos State, Nigeria</p>
                    </div>
                    <div class="contact-item">
                        <h3>Email</h3>
                        <a href="mailto:Qudusahmod@gmail.com">Qudusahmod@gmail.com</a>
                    </div>
                    <div class="contact-item">
                        <h3>LinkedIn</h3>
                        <a href="https://www.linkedin.com/in/qudus-damilare/" target="_blank">linkedin.com/in/qudus-damilare</a>
                    </div>
                </div>
                <form class="contact-form" onsubmit="handleSubmit(event)">
                    <div class="form-group">
                        <label for="name">Name</label>
                        <input type="text" id="name" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="subject">Subject</label>
                        <input type="text" id="subject" name="subject" required>
                    </div>
                    <div class="form-group">
                        <label for="message">Message</label>
                        <textarea id="message" name="message" required></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">Send Message</button>
                </form>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-info">
                    <h3>Qudus A.</h3>
                    <p>Growth Architect • Serial Intrapreneur</p>
                </div>
                <div class="footer-copyright">
                    © 2026 Qudus A. All rights reserved.
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Form submission handler
        function handleSubmit(e) {
            e.preventDefault();
            alert('Thank you for your message! This is a demo form. To make it functional, you would need to integrate a backend service or form handler like Formspree, EmailJS, or Netlify Forms.');
            e.target.reset();
        }

        // Add active state to navigation on scroll
        window.addEventListener('scroll', () => {
            const sections = document.querySelectorAll('section[id]');
            const scrollY = window.pageYOffset;

            sections.forEach(section => {
                const sectionHeight = section.offsetHeight;
                const sectionTop = section.offsetTop - 100;
                const sectionId = section.getAttribute('id');
                const navLink = document.querySelector(`.nav-links a[href="#${sectionId}"]`);

                if (scrollY > sectionTop && scrollY <= sectionTop + sectionHeight) {
                    navLink?.classList.add('active');
                } else {
                    navLink?.classList.remove('active');
                }
            });
        });
    </script>
</body>
</html>